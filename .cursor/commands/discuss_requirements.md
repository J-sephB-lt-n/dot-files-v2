# Discuss Requirements

Read through all of the documentation in `current_feature/`, starting with `current_feature/README.md`.

Then, talk me through the creation of a requirements document `current_feature/feature_requirements.md`, asking me pointed questions to explore and elucidate my requirements for the current feature being worked on.

Specifically, we should discuss the following:

Add a note to `current_feature/README.md` with a link to this requirements document which we've created.
